═══════════════════════════════════════════════════════════════════════════════════
  ADD COLUNA OBSERVATION NO SUPABASE
═══════════════════════════════════════════════════════════════════════════════════

📍 PASSO A PASSO (5 MINUTOS):

1️⃣  Abra o Supabase
    → Vá para: https://supabase.com
    → Faça login com sua conta
    → Selecione seu projeto

2️⃣  Vá para SQL Editor
    → Lado esquerdo (painel verde)
    → Clique no ícone de "SQL Editor" (parece um código)
    → Ou procure por "SQL Editor" no menu

3️⃣  Crie uma nova query
    → Clique em "New query" (botão azul no topo)

4️⃣  Cole este SQL:
    ┌───────────────────────────────────────────────────────────────┐
    │ ALTER TABLE key_history                                       │
    │ ADD COLUMN IF NOT EXISTS observation TEXT DEFAULT NULL;       │
    └───────────────────────────────────────────────────────────────┘

5️⃣  Execute
    → Clique em "RUN" (botão azul)
    → Ou pressione: Ctrl + Enter

6️⃣  Confirme o sucesso
    → Você deve ver: "Executing query... Successfully executed"
    → Se não houver erro, está feito!

═══════════════════════════════════════════════════════════════════════════════════

✅ DEPOIS DE ADICIONAR, RODE:

  cd backend
  node scripts/setup-observation.js

Isso vai confirmar que a coluna foi criada com sucesso.

═══════════════════════════════════════════════════════════════════════════════════

❓ AINDA TEM DÚVIDA?

Se der erro ao executar o SQL no Supabase:
  → Erro comum: "Column already exists"
    Isso significa que a coluna JÁ foi criada! É sucesso!
  
  → Erro: "Syntax error"
    Verifique se copiou o SQL exatamente como está acima

═══════════════════════════════════════════════════════════════════════════════════

🎯 SEU OBJETIVO:

Depois de executar o SQL no Supabase, a tabela key_history terá:
  ✓ id (uuid)
  ✓ key_id (uuid)
  ✓ instructor_id (uuid)
  ✓ withdrawn_at (timestamp)
  ✓ returned_at (timestamp)
  ✓ status (text: "active" ou "returned")
  ✓ observation (text) ← NOVO!
  ✓ created_at
  ✓ updated_at

═══════════════════════════════════════════════════════════════════════════════════
