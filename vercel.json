{
  "version": 2,
  "buildCommand": "",
  "installCommand": "npm install && cd backend && npm install",
  "devCommand": "cd backend && npm run dev",
  "env": {
    "NODE_ENV": "production",
    "PORT": "3000"
  }
}
